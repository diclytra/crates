set -g mouse on

unbind C-b
set-option -g prefix M-Space
bind M-Space send-prefix

set -g status-interval 1
set -g automatic-rename on
set -g automatic-rename-format "#{pane_index}"
set -g status-right "%Y-%m-%d %H:%M "

bind r source-file ~/.tmux.conf
